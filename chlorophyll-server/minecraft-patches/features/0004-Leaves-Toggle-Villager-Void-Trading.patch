From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: violetc <58360096+s-yh-china@users.noreply.github.com>
Date: Wed, 26 Jan 2022 17:20:54 +0800
Subject: [PATCH] Leaves Toggle Villager Void Trading

Biquaternions node:
This patch is an adaptation of the Leaves patch (only real diference in MerchantMenu).
It makes Paper PR #5047 toggleable, basically re-enabling the exploit.

diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index 86906f3425669a5a8bae49c57fcb08a48f2ffe3e..916a58ba348de2f78dd4feb175e72e8800114916 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -2920,6 +2920,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
             // Spigot start
             if (entity.getBukkitEntity() instanceof org.bukkit.inventory.InventoryHolder && (!(entity instanceof ServerPlayer) || entity.getRemovalReason() != Entity.RemovalReason.KILLED)) { // SPIGOT-6876: closeInventory clears death message
                 // Paper start - Fix merchant inventory not closing on entity removal
+                if (me.biquaternions.chlorophyll.ChlorophyllConfig.TOGGLES.PAPER.VILLAGER_VOID_TRADING_FIX) // Chlorophyll - Toggle villager void trading fix
                 if (entity.getBukkitEntity() instanceof org.bukkit.inventory.Merchant merchant && merchant.getTrader() != null) {
                     merchant.getTrader().closeInventory(org.bukkit.event.inventory.InventoryCloseEvent.Reason.UNLOADED);
                 }
diff --git a/net/minecraft/server/players/PlayerList.java b/net/minecraft/server/players/PlayerList.java
index 82748af2479b0f8f15e39d1ad1ccd2a0e583bcdd..faac130ee69c06724dfdd8f49d00b6b02e9e9b08 100644
--- a/net/minecraft/server/players/PlayerList.java
+++ b/net/minecraft/server/players/PlayerList.java
@@ -476,6 +476,7 @@ public abstract class PlayerList {
                 player.stopRiding();
                 vehicle.getPassengersAndSelf().forEach(e -> {
                     // Paper start - Fix villager boat exploit
+                    if (me.biquaternions.chlorophyll.ChlorophyllConfig.TOGGLES.PAPER.VILLAGER_VOID_TRADING_FIX) // Chlorophyll - Toggle villager void trading fix
                     if (e instanceof net.minecraft.world.entity.npc.villager.AbstractVillager villager) {
                         final net.minecraft.world.entity.player.Player human = villager.getTradingPlayer();
                         if (human != null) {
diff --git a/net/minecraft/world/inventory/MerchantMenu.java b/net/minecraft/world/inventory/MerchantMenu.java
index 1dd82dfa739957f00d51c8b987289a002815f622..4966284bbb35dd86fe326f904642fd959eff4eb5 100644
--- a/net/minecraft/world/inventory/MerchantMenu.java
+++ b/net/minecraft/world/inventory/MerchantMenu.java
@@ -74,6 +74,7 @@ public class MerchantMenu extends AbstractContainerMenu {
 
     @Override
     public boolean stillValid(final Player player) {
+        if (me.biquaternions.chlorophyll.ChlorophyllConfig.TOGGLES.PAPER.VILLAGER_VOID_TRADING_FIX) // Chlorophyll - Toggle villager void trading fix
         if (!checkReachable) return true; // Paper - checkReachable
         return this.trader.stillValid(player);
     }
