name: Build 26.1.2
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - "ver/**"

jobs:
  build:
    runs-on: ubuntu-latest
    if: "!contains(github.event.commits[0].message, '[ci skip]')"
    permissions:
      contents: write
    env:
      BUILD_NUMBER: ${{ github.run_number }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: 'gradle'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Configure Git
        run: |
          git config --global user.email "no-reply@github.com"
          git config --global user.name "Github Actions"

      - name: Apply Patches
        run: ./gradlew applyAllPatches --stacktrace

      - name: Create Paperclip Jar
        run: ./gradlew createPaperclipJar --stacktrace

      - name: Prepare release notes and artifacts
        run: |
          chmod +x ./scripts/prepareRelease.sh
          ./scripts/prepareRelease.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPO: ${{ github.repository }}
          BUILD_NUMBER: ${{ env.BUILD_NUMBER }}

      - name: Upload Chlorophyll
        uses: actions/upload-artifact@main
        with:
          name: Chlorophyll 26.1.2
          path: ./chlorophyll-server/build/libs/chlorophyll-paperclip-*.jar

      - name: Release Chlorophyll
        uses: softprops/action-gh-release@master
        with:
          name: "🍃 Chlorophyll 26.1.2 (Build ${{ env.BUILD_NUMBER }})"
          tag_name: "ver-26.1.2"
          files: |
            ./chlorophyll-server/build/libs/chlorophyll-paperclip-*.jar
            ./chlorophyll-api/build/libs/chlorophyll-api-*.jar
          body_path: "./RELEASE.md"
          token: "${{ secrets.GITHUB_TOKEN }}"
          target_commitish: "${{ github.sha }}"
          draft: false
          prerelease: false

      - name: Publish Chlorophyll Version
        uses: fjogeleit/http-request-action@v1
        with:
          url: https://version.serlith.net/submit
          method: 'POST'
          customHeaders: '{"Content-Type": "application/json", "Authorization": "${{ secrets.VERSION_TOKEN }}", "Server-Software": "Chlorophyll"}'
          data: '{ "software": "Chlorophyll", "version": "26.1.2", "build": ${{ env.BUILD_NUMBER }} }'
